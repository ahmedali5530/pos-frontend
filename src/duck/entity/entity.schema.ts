import {schema} from 'normalizr';


export const UserAccountSchema = new schema.Entity('userAccount', {});

export const StoreSchema = new schema.Entity('store', {});

export const ShortcutSchema = new schema.Entity('shortcut', {});

export const TouchSchema = new schema.Entity('touch', {});
